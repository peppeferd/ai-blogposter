import React from "react";
export const metadata = {
  title: "Posts | Bloggify",
  description: "AI Next js - Create post",
};

const layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};
export default layout;
